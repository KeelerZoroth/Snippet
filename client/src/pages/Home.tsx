import "../assets/styles/home.css"; // Import CSS

const Home = () => {
    return (
        <section className="home-container">
            <h1></h1>
        </section>
    );
};

export default Home;
